// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://rilesmag.com',
  // Custom domain serves from the root, so no `base` path is needed.
});
